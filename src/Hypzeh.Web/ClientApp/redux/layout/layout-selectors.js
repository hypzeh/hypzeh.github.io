export const getLayoutName = (state) => (state.layout.name);

export const getSidebar = (state) => (state.layout.sidebar);

export const isHeaderEnabled = (state) => (state.layout.header.enabled);

export const isNavbarEnabled = (state) => (state.layout.navbar.enabled);

export const isSidebarEnabled = (state) => (state.layout.sidebar.enabled);
