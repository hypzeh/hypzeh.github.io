import React from 'react';

import { PrimarySection } from '../../shared/styled/sections';

const AboutDisplay = () => (
  <PrimarySection>
    <span>About.</span>
  </PrimarySection>
);

export default AboutDisplay;
